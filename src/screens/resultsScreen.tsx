import React from "react";
import { Layout } from "react-native-ui-kitten";
import ResultsPage from "../designs/ResultsPage";

const ResultsScreen = () => (
    <Layout style={{ flex: 1, justifyContent: "center" }}>
        <ResultsPage />
    </Layout>
);

export default ResultsScreen;
